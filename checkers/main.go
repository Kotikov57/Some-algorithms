package main

import(
	"fmt"
	"bufio"
	"os"
)

func captureableWhite(i, j, n, m int, array [][]int) bool{
	if i > 1 {
		if j > 1 && array[i + 1][j - 1] == 2 && array[i - 2][j - 2] == 0{
			return true
		}
		if j < m - 1 &&{

		}
	}
}